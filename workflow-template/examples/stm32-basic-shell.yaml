project:
  name: stm32-basic-shell
  mcu: STM32F407VET6
  toolchain: STM32CubeCLT
  build_dir: build/Debug
  elf: build/Debug/basic-shell.elf

build:
  command: ninja
  working_dir: build/Debug

flash:
  tool: STM32_Programmer_CLI
  interface: SWD
  verify: true
  reset: true

serial:
  shell_port: COM4
  baudrate: 115200

tests:
  - name: shell
    script: tools/test_shell.ps1
