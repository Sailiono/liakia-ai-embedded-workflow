# Known-Bad App-Layer Redirect

Known-bad code is now organized as complete lab packs under:

```text
../../known-bad-cases/
```

Use the case folders there instead of importing fragments from this legacy location. Each case folder contains:

- the intentionally broken code to import;
- a practice guide;
- a separate answer key.

Recommended first case:

[Case B: BMP280 data quality failure](../../known-bad-cases/case-b-bmp280-calibration/README.md)
